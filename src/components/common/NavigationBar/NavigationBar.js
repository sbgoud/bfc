// src/components/common/NavigationBar.js
import React from 'react';

const NavigationBar = () => {
  return (
    <nav className="navbar">
      {/* Add navigation links, logo, etc. later */}
    </nav>
  );
};

export default NavigationBar;